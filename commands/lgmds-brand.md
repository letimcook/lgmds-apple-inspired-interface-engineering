---
description: Animate a logo, brand mark or splash screen (LGMDS)
argument-hint: [logo/brand asset and animation intent]
---

Read `SKILL.md` and `references/brand-motion.md` from the LGMDS skill, then run the required sequence (source → vector/geometry audit → overlay validation → semantic decomposition → motion brief → animation → deterministic frame QA → final-frame comparison) for: $ARGUMENTS
