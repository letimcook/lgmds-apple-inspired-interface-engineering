---
description: Implement Liquid Glass/Glassmorphism in HTML/CSS/JS/GSAP/WebGL (LGMDS)
argument-hint: [web component or page to implement]
---

Read `SKILL.md` and `references/web-motion.md` from the LGMDS skill, then implement this web component choosing CSS, GSAP or WebGL/SVG refraction per the runtime-selection rules, always with a solid/reduced-effects fallback: $ARGUMENTS
